apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
  namespace: angular-recipe
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    component: web
